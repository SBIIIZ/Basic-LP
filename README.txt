Before trying to run this REACT app follow these steps:

- open temrinal in main folder "Basic LP" and run this command = npm install
- npx vite build
- npm run dev to launch the app form local machine